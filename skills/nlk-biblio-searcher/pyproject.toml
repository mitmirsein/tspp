[project]
name = "nlk-biblio-searcher"
version = "1.0.0"
description = "국립중앙도서관(NLK) 서지 검색 CLI — 제목/저자/ISBN 조회 (RISS 국내 학술 검색 흡수)"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
]

[tool.uv]
# uv run python scripts/search.py 로 스킬 디렉터리에서 직접 실행
